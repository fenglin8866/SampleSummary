package com.sample.core.basic.test

class Test {

    init {

       /* lifecycleScope.launch {
            viewModel.consumeIntents(
                merge(
                    intentSubmitFlow,
                    intentRefreshFlow,
                    intentRetryFlow
                )
            )
        }*/
    }

    /*val intentSubmitFlow = binding.submitButton.clicks()
        .map { MainUIIntent.SubmitOrder(id = "123") }

    val intentRefreshFlow = binding.swipeRefreshLayout.refreshes()
        .map { MainUIIntent.RefreshData }

    val intentRetryFlow = binding.retryButton.clicks()
        .map { MainUIIntent.Retry }*/


}