项目介绍
显示任务列表。
核心元素：标题，日期，优先级，是否完成。
要求：
1.提供按钮，是否显示完成的任务
2.提供优先级按钮，显示的任务列表是否按优先级排序。
3.提供日期按钮，显示的任务列表是否按日期排序。
4.当优先级按钮和日期按钮同时选中时，先按日期排序，日期相同的再按优先级排序。

DataStore使用
1.构建DataStore单例，可配置是否兼容SharedPreferences
2.选择使用类型
a.PreferenceDataStore
	1.定义存储Key，定义获取值构建的对象（可选）
	2.读写操作：dataStore.data.map{preferences->{ val name=preferences[keys] } }
	3.写入操作:dataStore.edit{preferences->{ preferences[keys]="" }
	          dataStore.updateData {
                      it.toMutablePreferences().also { preferences ->
                          preferences[keys]=""
                      }
                  }
b.JsonDataStore
    1.定义类型，进行序列化。
    2.定义序列化器
        a.设置默认值
        b.写操作：output.write(Json.encodeToString(t)..encodeToByteArray())
        c.读操作:Json.decodeFromString<UserPreferences>(input.readBytes().decodeToString())
    3.读写操作：dataStore.data.map{preferences->{ val name=preferences.name } }
    4.写入操作:dataStore.updateData { preferences ->  preferences.copy(sortOrder = newSortOrder)}
c.ProtoDataStore
    1.创建Proto文件，定义类型
        注意：proto文件定义在main中的proto包下，不能放在java包中
    2.定义序列化器，生成类型包含对象的序列化方法。
        a.设置默认值
        b.写操作：t.writeTo(output)
        c.读操作: UserPreferences.parseFrom(input)
    3.读写操作：dataStore.data.map{ preferences-> { val name=preferences.name } }
    4.写入操作:dataStore.updateData { preferences-> preferences.toBuilder().setSortOrder(newSortOrder).build()}
             dataStore.updateData { preferences->  preferences.copy { sortOrder = newSortOrder  }}
注意：如果proto文件没生成，删除build文件在重新生成试试。
生成文件路径：build/generated/sources/proto

原则
1.请勿在同一进程中为给定文件创建多个 DataStore 实例，否则会破坏所有 DataStore 功能。
如果给定文件在同一进程中有多个有效的 DataStore 实例，DataStore 在读取或更新数据时将抛出 IllegalStateException。
注意：同一进程中同一个文件只能有一个实例，否则会抛出 IllegalStateException；不是指同一进程只有一个DataStore。
2.DataStore<T> 的泛型类型必须不可变。更改 DataStore 中使用的类型会导致 DataStore 提供的一致性失效，并且可能会造成严重的、难以发现的 bug。
我们建议您使用协议缓冲区，这有助于确保不可变性、清晰的 API 和高效的序列化。
3.切勿对同一个文件混用 SingleProcessDataStore 和 MultiProcessDataStore。如果您打算从多个进程访问 DataStore，则必须使用 MultiProcessDataStore。

实践
1.涉及存储DataStore的数据比较多，而且涉及不同的功能，冷启动的时候没必要全部创建，建议按功能建独立的 DataStore 文件。
    延迟初始化
    通过 by lazy 或依赖注入（如Hilt）在需要时才构建
    避免在Application中一次性创建所有DataStore
    注意事项
    每个 DataStore 需要不同的文件名（name参数）
    同一文件名的 DataStore 在同一进程中应保持单例，避免多次创建
    建议按功能模块划分，而非每个页面都创建（避免过多文件)
2.定义的Preference对象和PreferencesKeys是集中在一处管理，还是在PreferencesRepository中管理?
    建议在对应的模块中管理。

3.多个DataStore怎么组合使用？
https://my.feishu.cn/wiki/XQu8wcOK6iMhRlkFSrHczjKAnoc?from=from_copylink

4.PreferenceDataStore存了很多配置，当其他一个变化时，会导致Flow变化，没变化配置怎么过滤？
https://my.feishu.cn/wiki/XQu8wcOK6iMhRlkFSrHczjKAnoc?from=from_copylink

其他
1.SharedPreferences迁移DataStore
参考“SharedPreferencesMigrationUtil”

2.同步代码使用DataStore。
val exampleData = runBlocking { context.dataStore.data.first() }
lifecycleScope.launch {
        context.dataStore.data.first()
        // You should also handle IOExceptions here.
    }
不需要切换线程，因为 dataStore.data 是 Flow，可以在任何线程中使用。
https://my.feishu.cn/wiki/EstGwfK6CiN7G3k93wzcXU3knXc?from=from_copylink

3.处理文件损坏
https://my.feishu.cn/wiki/NoNJwFbYBiBA6Dk8X9vcoleYnPh

4.数据是否安全
https://my.feishu.cn/wiki/OUPLwcts6icjxkkTBj4cotlWnwh?from=from_copylink

5.多进程使用DataStore，只是构建DataStore有差异，其他操作保持不变。
val dataStore = MultiProcessDataStoreFactory.create(
    serializer = TimeSerializer,
    produceFile = {
        File("${context.cacheDir.path}/time.pb")
    },
    corruptionHandler = null
)



======================================
项目启示
1.将两个按钮合并为一个状态。不要使用两个布尔值来表示排序顺序，而是使用一个枚举值，减少状态管理。例如：优先级和时间排序
2.配置与数据分不同的仓库管理。通过不同的配置组合，过滤，排序显示不同的数据。例如：Task+UserPreferences
3.构建UI显示数据模型，将不同的数据合并为UI要显示的数据。例如：TasksUiModel
4.架构依赖要清晰，尽量保持单向依赖。例如：UI层，数据层，UI不涉及任何DataStore的操作，全部封装在Repository层。
5.SharedPreferences的使用，通过StateFlow封装构建响应式。只需在冷启动读取一次，其他操作都是更新，减少了反复读取操作。
3.防抖的实现。分不同的场景a.本地操作,使用debounce时间间隔 + 用户提示。b.长耗时操作（网络，大文件处理），根据操作完整状态 + 用户提示。
https://my.feishu.cn/wiki/Ko2gwEXADiXyggkzDCPcob85nLh?from=from_copylink
------------------------------------------
架构设计
1.不同层级的数据转换，使用map
2.多个Flow组合，使用combine
3.将Repository中Flow转换为ViewModel中的StateFlow
https://my.feishu.cn/wiki/JiCEw9A6cip93DkEYvgcGdkanOR?from=from_copylink
4.使用State封装UI状态，UI组件响应State变化。
5.点击封装为用户意图处理。
6.当UI层需要使用多个Repository时，建议使用ViewModel作为中间层，将多个Repository的数据进行组合，并提供给UI层。当ViewModel比较复杂时，可以抽象一个Domain层处理
============================

拆坑点
问题：执行时序问题，当一些View状态与配置管理关联时，容易出现死循环。例如：switch按钮。

原因：
先设置监听后，按钮为默认状态，再设置按钮状态，会触发监听，导致无限循环。

collect 收到数据
    ↓
updateTaskFilters() 更新按钮状态（设置 isChecked）
    ↓
触发 OnCheckedChangeListener
    ↓
调用 viewModel.enableSortByDeadline() 等方法
    ↓
修改 DataStore → userPreferencesFlow 发射新值
    ↓
combine 重新计算 → tasksUiModel2 发射新值
    ↓
回到 collect，形成无限循环

解决
先设置按钮状态，再设置监听。
